/**
 * USD → KHR conversion for reporting (gifts, exports, dashboards).
 * Keep in sync with frontend `frontend/lib/gift-exchange.ts` and product copy.
 */
export const USD_KHR_EXCHANGE_RATE = 4000 as const;
